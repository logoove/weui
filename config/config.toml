# HTTP Server
[server]
	Address     = ":8885"
	ServerAgent = "gf-app"
	LogPath     = "tmp"

# Logger.
[logger]
    Path        = "tmp/log"
    Level       = "all"
    Stdout      = false
[database]
    type = "sqlite"
    link  = "weui/db.db"
    debug = false
    # Database logger.
    [database.logger]
        Path   = "tmp/log"
        Level  = "all"
        Stdout = false
